apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: moni
  labels:
    app: node-exporter
spec:
  type: ClusterIP
  selector:
    app: node-exporter
  ports:
    - name: node-metrics
      port: 9100
      targetPort: 9100